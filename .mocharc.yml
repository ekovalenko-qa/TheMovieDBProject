color: true
diff: true
extension:
  - 'js'
require:
  - '@babel/register'
reporter: 'spec'
slow: 300
sort: false
spec: './tests/api/*.api_test.js' # the positional arguments!
timeout: false # same as "no-timeout: true" or "timeout: 0"
ui: 'bdd'
